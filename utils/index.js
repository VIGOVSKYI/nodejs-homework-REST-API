const wrapperControlers = require("./wrapperConrtollers");
const validateBody = require("./validateBody");
const handleMongooseError = require("./handleMongooseError");

module.exports = {
    wrapperControlers,
    validateBody,
    handleMongooseError,
}